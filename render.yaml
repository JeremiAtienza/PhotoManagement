services:
  - type: web
    name: photoflow
    env: python
    rootDirectory: "."
    buildCommand: python -m pip install -r requirements.txt && python manage.py collectstatic --noinput
    preDeployCommand: python manage.py migrate
    startCommand: gunicorn photoalbum.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DJANGO_ENV
        value: production
      - key: DEBUG
        value: true
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: photomanagement.onrender.com,*.onrender.com,127.0.0.1,localhost
      - key: DATABASE_URL
        fromDatabase:
          name: photoflow-db
          property: connectionString
      - key: CLOUDINARY_URL
        value: cloudinary://271951834154829:RLbxKrJlP5WYzkafv2C0K22zrOw@dsmjr2y5c
      - key: CSRF_TRUSTED_ORIGINS
        value: https://photomanagement.onrender.com

databases:
  - name: photoflow-db
    databaseName: photoflow
    user: photoflow
